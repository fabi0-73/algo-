# AMD Strategy Backtester - Environment Configuration
# Copy this file to .env and fill in your actual credentials
# NEVER commit .env to version control!

# =============================================================================
# MetaTrader 5 Configuration
# =============================================================================
# Your MT5 account login number
MT5_LOGIN=12345678

# Your MT5 account password (keep secret!)
MT5_PASSWORD=your_password_here

# Your broker's MT5 server name
MT5_SERVER=YourBroker-Server

# Optional: Path to terminal64.exe if not in default location
# MT5_PATH=C:/Program Files/MetaTrader 5/terminal64.exe

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=amd_trading
DB_USER=postgres
DB_PASSWORD=your_db_password_here

# =============================================================================
# Timezone Configuration
# =============================================================================
# IMPORTANT: MT5 timestamps are typically naive (no timezone info).
# Set this to match your broker's server timezone.
# Common broker timezones:
#   - UTC (many brokers)
#   - Europe/Athens (GMT+2/+3 for some brokers)
#   - America/New_York (some US brokers)
# If unsure, check your MT5 terminal's Market Watch time.
DATA_TIMEZONE=UTC

# Session filter evaluation timezone (usually UTC for consistency)
SESSION_TIMEZONE=UTC
